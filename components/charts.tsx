'use client'

import {
  AreaChart as ReAreaChart,
  Area as ReArea,
  BarChart as ReBarChart,
  Bar as ReBar,
  XAxis as ReXAxis,
  YAxis as ReYAxis,
  CartesianGrid as ReCartesianGrid,
  Tooltip as ReTooltip,
  ResponsiveContainer as ReResponsiveContainer,
  Cell as ReCell,
} from 'recharts';

export const AreaChart = ReAreaChart;
export const Area = ReArea;
export const BarChart = ReBarChart;
export const Bar = ReBar;
export const XAxis = ReXAxis;
export const YAxis = ReYAxis;
export const CartesianGrid = ReCartesianGrid;
export const Tooltip = ReTooltip;
export const ResponsiveContainer = ReResponsiveContainer;
export const Cell = ReCell;
